# Supabase Configuration
# Get these from your Supabase project settings (https://supabase.com/dashboard/project/_/settings/api)

# Public Supabase URL (safe to expose to client)
VITE_SUPABASE_URL=your-project-url

# Public Anon Key (safe to expose to client)
VITE_SUPABASE_ANON_KEY=your-anon-key

# Service Role Key (KEEP SECRET! Only use in server/scripts, never expose to client)
# This is needed for the scraping script to bypass RLS policies
SUPABASE_SERVICE_KEY=your-service-role-key
